# 2026-07-09

- 新增拼图联机游戏 PuzzleCoop.tsx：纯网页拼图 + PeerJS P2P 多人合作
- 路由 /puzzle-coop，已注册到 tools.ts 和 GameHub GAME_LIBRARY（noSandbox）
- 联机方案：PeerJS P2P 星型拓扑，房主中继，4位房间码，状态/光标/聊天同步，拖拽锁防冲突
- 安装 peerjs@1.5.5（用 --legacy-peer-deps，项目 react-emulatorjs peer 冲突）
- 修复拼图游戏白屏：PeerJS 1.5.5 ESM 导出中没有 DataConnection 命名导出（只有 DataConnectionErrorType 枚举），`import { DataConnection }` 运行时报错导致组件加载失败。改用 `import type { DataConnection }` 解决——类型定义(types.d.ts)有声明但运行时未导出
- 修复拼豆游戏画布空白/无法点击：React useEffect 执行顺序导致 `boardRef.current` 在 `render()` 调用时尚未更新（board 状态已设置，但 ref 更新 useEffect 在 render useEffect 之后执行），Canvas 错过重绘且保持空白。合并 boardRef/cursorsRef 更新与 render 到同一 useEffect 内，确保先更新 ref 再调用 render
- 修复联机崩溃：`board.cells` 类型从 `Int8Array` 改为 `number[]`，避免 PeerJS 传输后类型退化/浏览器对 TypedArray.filter 不支持导致 `board.cells.filter is not a function` 崩溃；接收 welcome/newboard 时用 `Array.from(cells)` 归一化
- 修复联机同步失效根因：PeerJS 事件回调闭包捕获了 createRoom/joinRoom 执行时的 handleMsg，setRole('host') 异步导致 role 仍为 null，handleMsg 内 isHost=false → 房主收到 hello 不发 welcome → 加入者卡死。修复：handleMsg/broadcast 改用 roleRef.current，peer 回调用 handleMsgRef.current 调用最新版本
- 拼豆游戏新增共享参考图纸功能：房间内成员可上传最多3张参考图，P2P同步给全员，点击放大查看，支持删除
- 拼豆游戏新增自定义豆子颜色：调色板支持添加/删除自定义颜色，房间内同步共享调色板
- 构建并增量部署到 110.42.247.238
- 拼豆画板扩展到 1024×1024：新增 64/128/256/512/1024 尺寸选项，分级渲染（≤64 完整圆+高光+钉子，65~256 简化圆，>256 ImageData Uint32 像素直写），大网格拖拽直接突变 boardRef+drawSingleCell 单格重绘延迟 React 状态更新，applyOps/clearBoard/handleMsg clear 统一用 boardRef 避免 100万元素数组拷贝，filledCount 用 useMemo，canvasSize 动态 max(640,min(2048,gridSize*4))
- 修复三个问题：(1)changeSize/loadPreset/clearBoard/onCanvasMove/onCanvasUp/sendChat 全部改用 multiplayerRef/roleRef/myIdRef 避免闭包过期导致 broadcast 不执行 (2)大网格 cursor 不触发 setCursors + 拆分 render useEffect（cursor 变化仅 ≤256 网格重渲染），避免加入者每 50ms 全量 ImageData 重渲染卡死 (3)新增 6 个复杂预设（棋盘/彩虹/同心圆/大爱心/渐变/星空）+ ≥512 画板支持 1~8x 缩放（按钮+滚轮）+ 滚动画布容器
- 取消 512/1024 画板及所有相关特殊实现：删除 BOARD_SIZES 中 512/1024 选项、render 中 gs>256 ImageData 像素直写分支、drawSingleCell 中 gs>256 分支、缩放控制 UI（≥512）、canvas wheel 缩放事件和 zoom 样式、zoom 状态及所有 setZoom 调用、ZoomIn/ZoomOut 导入、菜单 ≥512 警告、canvasSize 中 Math.min(2048) 上限；简化 gs<=256 恒真条件为无条件；tsc 零错误通过
- 修复 128/256 大画板联机失败：根因是 welcome 消息包含完整 cells 数组（256×256=65536个数字约200KB），超过 WebRTC DataChannel 单消息限制导致消息丢失。修复：添加 RLE 编解码函数 rleEncode/rleDecode，welcome/newboard 消息改用 cellsRLE 字段传输压缩数据，空 256 画板从 ~200KB 压缩到 ~15 字节
- 房主退出自动转移：房主退出时选最先进来的在线玩家作为新房主，发送 hostmigrate 消息；新房主销毁旧 peer 后用 PEER_PREFIX+code 注册新 peer（重试5次等待旧 ID 释放），其他玩家3秒后重连（重试8次）；hello 处理支持玩家重连不重复添加；意外断开时最早加入者自动成为新房主
- 拼豆进度本地保存/导出/导入：30秒自动保存到 localStorage(AUTOSAVE_KEY)、手动保存(SAVE_KEY 最多20条)、JSON 文件导出/导入、菜单页显示自动保存恢复和已保存项目列表（载入/删除）
- 拼豆参考图纸导入为豆图：在参考图悬停菜单添加"导入为豆图"按钮（房主/单人可用），自动识别图片网格尺寸与颜色并映射到当前调色板，支持预览、手动调整画板尺寸、重新识别；应用后更新画板并同步房间（房主持 newboard 消息）
- 共享听歌房 MusicRoom.tsx 完成原型图3栏布局改造并部署：左侧(热门/搜索Tab+歌曲列表+待播放歌单)，中间(大封面/视频区+弹幕层+歌词滚动+播放控制+聊天输入+快捷表情)，右侧(房间信息+用户网格+游戏/礼物Tab+游戏网格+礼物网格+离开按钮)。新增弹幕系统(CSS @keyframes danmaku-scroll)、LRC歌词同步解析与滚动、30首热门歌曲列表、房间名称编辑同步、roomname消息类型。tsc零错误，vite build成功，deploy.sh frontend -s -i 增量部署到 110.42.247.238
