# 2026-07-14

- 新增共享听歌房 MusicRoom.tsx：PeerJS P2P 房间系统（密码入房、房主迁移）、同步播放器（房主控制、客户端时间戳同步）、多平台搜歌（网易云/QQ音乐/酷狗）、歌单队列（切歌/上下移动/列表循环/单曲循环/随机播放）、实时聊天、表情弹幕飘屏、刷礼物互动、三种房间小游戏（摇骰子/十五二十/小姐牌）
- 音乐 API 代理：vite.config.ts 添加 /music-api/netease、/music-api/qq、/music-api/kugou 代理；nginx.conf 同步添加生产环境代理（proxy_pass + proxy_ssl_server_name）
- 路由 /music-room，已注册到 tools.ts（Headphones 图标）和 router.tsx SEO
- 网易云搜索：/music-api/netease/api/search/get?s=xxx&type=1&limit=30；播放：https://music.163.com/song/media/outer/url?id={id}.mp3
- 服务器磁盘满（40G/100%）：清理 /tmp 下 15+ 个 html-backup-* 目录和旧 tar.gz 文件，释放 19G 空间
- 部署方式：deploy.sh frontend -s -i 增量 rsync；nginx.conf 通过 cat | ssh 管道部署 + nginx -s reload
- 根据原型图改造 MusicRoom.tsx 为3栏布局：左侧(热门/搜索Tab+歌曲列表+待播放歌单2/5高度)，中间(大封面/视频区+弹幕层+歌词滚动+播放控制+聊天输入+8个快捷表情)，右侧(房间信息+房间名编辑+用户网格4列+游戏/礼物Tab+游戏网格4列+礼物网格4列+离开按钮)。新增弹幕系统(CSS @keyframes danmaku-scroll)、LRC歌词同步(parseLRC+fetchLyrics+300ms定时器)、30首热门歌曲列表、房间名编辑同步(roomname消息)、默认Tab设为rank
- 听歌房新增霸榜功能：RankEntry类型(songsAdded/chatCount/giftsSent/gamePlays/total)，rankstats消息类型同步。房主维护统计(bumpStat函数)，各动作函数(addToPlaylist/sendChat/sendEmoji/sendGift/startGame/diceRoll/drawCard/fifteen20Action)埋点，handleMsg中为客人行为统计(addsong/chat/game)。welcome消息包含rankStats。右侧栏新增"霸榜"Tab(默认选中)，含5个分类(总榜/点歌/聊天/礼物/游戏)，Top3奖牌🥇🥈🥉，积分规则(点歌×10/聊天×1/礼物×5/游戏×3)。修复game消息未中继问题(relayToOthers)。tsc零错误，vite build成功，已部署
